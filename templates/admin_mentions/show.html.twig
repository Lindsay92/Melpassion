{% extends 'base-admin.html.twig' %}

{% block title %}Mentions Légales{% endblock %}

{% block body %}
    <h1>Voir : Mentions Légales</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ mention.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ mention.title }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ mention.description|raw }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_mentions_index') }}"class="btn btn-warning">Retour à la liste</a>

    <a href="{{ path('admin_mentions_edit', {'id': mention.id}) }}" class="btn btn-success">Mettre à jour</a>

    {{ include('admin_mentions/_delete_form.html.twig') }}
{% endblock %}
