<form method="post" action="{{ path('admin_astuces_delete', {'id': astuce.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ astuce.id) }}">
    <button class="btn btn-danger my-2">Supprimer</button>
</form>
