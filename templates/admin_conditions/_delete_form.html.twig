<form method="post" action="{{ path('admin_conditions_delete', {'id': condition.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ condition.id) }}">
    <button class="btn btn-danger my-2">Supprimer</button>
</form>
