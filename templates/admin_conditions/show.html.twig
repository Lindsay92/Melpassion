{% extends 'base-admin.html.twig' %}

{% block title %}Conditions Générales{% endblock %}

{% block body %}
    <h1>Voir : Conditions Générales</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ condition.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ condition.title }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ condition.description|raw }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_conditions_index') }}" class="btn btn-warning">Retour à la liste</a>

    <a href="{{ path('admin_conditions_edit', {'id': condition.id}) }}" class="btn btn-success">edit</a>

    {{ include('admin_conditions/_delete_form.html.twig') }}
{% endblock %}
